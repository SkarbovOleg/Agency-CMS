version: '3.8'

services:
  cms:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./db.json:/app/db.json
      - ./public:/app/public
    restart: unless-stopped